amqp==5.3.1
asgiref==3.11.0
billiard==4.2.4
boto3==1.42.2
botocore==1.42.2
celery==5.6.0
click==8.3.3
click-didyoumean==0.3.1
click-plugins==1.1.1
click-repl==0.3.0
django==5.2.17
django-cleanup==9.0.0
django-otp==1.7.3
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-redis==5.4.0
django-storages==1.14.6
python-dotenv==1.2.2
PyJWT==2.15.0
python-dateutil==2.9.0.post0
redis==6.4.0
s3transfer==0.16.0
six==1.17.0
sqlparse==0.6.0
tzdata==2025.2
tzlocal==5.3.1
urllib3==2.0.7
vine==5.1.0
kombu==5.6.1
jmespath==1.0.1
packaging==25.0
prompt_toolkit==3.0.52
requests==2.33.0
channels==4.2.0
channels-redis==4.2.1
daphne==4.2.3
# Transitive via daphne[tls], pinned to floors that carry security fixes:
#   Twisted  PYSEC-2026-160   (fixed 26.4.0)
#   pyOpenSSL PYSEC-2026-2268/2269 (fixed 26.0.0)
# Left as floors rather than exact pins so daphne can move them forward.
Twisted>=26.4.0
pyOpenSSL>=26.0.0
setuptools>=84.0.0
whitenoise==6.9.0
firebase-admin==6.5.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9
# Core Payment Modules
cashfree-pg==3.2.12
pydantic>=1.10.5,<2.0.0
cryptography==50.0.1

# Geo (service-area enforcement). shapely is a pure-Python geometry
# library; no PostGIS extension required on the database side. Hyderabad
# service-area check uses a polygon defined in base/service_area.py.
shapely==2.0.6

sentry-sdk[django]==1.32.0

# PDF receipts (CGST Sec 31). reportlab is pure-Python so no system
# packages required on the deploy host — chosen over WeasyPrint which
# needs Cairo / Pango installed at the OS level.
reportlab==4.2.5
